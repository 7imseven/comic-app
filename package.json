{
  "name": "comic-reader",
  "version": "1.0.0",
  "description": "漫画阅读器",
  "main": "index.html",
  "scripts": {
    "build": "echo 'done'",
    "ios": "npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
